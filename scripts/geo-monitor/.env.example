# Copy to .env in the repo root (not this folder) and fill in whichever keys
# you have — citation-test.mjs skips any engine without a key. See README.md
# for where to get each one. Never commit the real .env file.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
PERPLEXITY_API_KEY=
GOOGLE_API_KEY=
